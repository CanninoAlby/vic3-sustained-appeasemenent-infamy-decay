pp_process_weekly_appeasement = {
    if = {
        limit = { has_modifier = modifier_positive_propaganda.1 }
        add_government_expenses = { value = pp_cost_tier_1 label = "pp_appeasement_pact" }
    }
    else_if = {
        limit = { has_modifier = modifier_positive_propaganda.2 }
        add_government_expenses = { value = pp_cost_tier_2 label = "pp_appeasement_pact" }
    }
    else_if = {
        limit = { has_modifier = modifier_positive_propaganda.3 }
        add_government_expenses = { value = pp_cost_tier_3 label = "pp_appeasement_pact" }
    }
}
