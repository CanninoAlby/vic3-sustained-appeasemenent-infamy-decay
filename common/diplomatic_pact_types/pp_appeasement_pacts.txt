pp_appeasement_pact = {
	is_read_only = yes
	is_subsidies = yes
	selectable_in_diplomatic_panel = no
	
	# What the player (first_country) pays
	cost = {
		value = second_country.gdp
		multiply = first_country.var:pp_appeasement_rate
	}

    # What the AI (second_country) receives
    income = {
        value = second_country.gdp
        multiply = first_country.var:pp_appeasement_rate
    }
}
